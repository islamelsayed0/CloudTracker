# Environment variables - Contains sensitive credentials and configuration
# SECURITY: Never commit these files to version control
.env                # Main environment variables file with API keys and secrets
.flaskenv           # Flask-specific environment variables

# Python - Bytecode, cache files, and virtual environments
# These are generated files that should not be version controlled
__pycache__/        # Python bytecode cache directory
*.py[cod]           # Python compiled files
*$py.class          # Python optimized bytecode
*.so                # C extension modules
.Python             # Python executable link in virtual environments
env/                # Old-style virtual environment
build/              # Build artifacts
develop-eggs/       # Development egg links
dist/               # Distribution packages
downloads/          # Downloaded packages
eggs/               # Egg format packages
.eggs/              # Egg cache
lib/                # Library files
lib64/              # 64-bit library files
parts/              # Buildout parts
sdist/              # Source distributions
var/                # Variable data
*.egg-info/         # Egg metadata
.installed.cfg      # Buildout installation config
*.egg               # Egg packages
venv/               # Virtual environment directory
.venv/              # Alternative virtual environment directory

# Flask - Flask-specific cache and instance files
instance/           # Flask instance folder (contains local configs)
.webassets-cache    # Webassets cache for static file management

# IDE specific files - Editor configurations and temporary files
.idea/              # JetBrains IDEs (PyCharm, IntelliJ, etc.)
.vscode/            # Visual Studio Code settings
*.swp               # Vim swap files
*.swo               # Vim swap files (alternative)

# Azure - Azure-specific configuration and cache files
.azure/             # Azure CLI configuration and cache

# Logs - Application logs and log directories
logs/               # Log directory
*.log               # Log files

# Database - Local database files
# SECURITY: Never commit database files with sensitive data
*.db                # SQLite database files
*.sqlite3           # SQLite3 database files

# OS specific - Operating system generated files
.DS_Store           # macOS folder metadata
Thumbs.db           # Windows thumbnail cache
